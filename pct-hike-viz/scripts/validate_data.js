#!/usr/bin/env node

// Keep the web command stable while making its assertion source the shared
// PCTA + USGS terrain contract used by both web and iOS.
import "../../scripts/validate_runtime_bundles.mjs";
